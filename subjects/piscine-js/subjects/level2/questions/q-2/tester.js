// ::javascript
// ::header
// ::code

//your code here

// ::footer
const process = null;

try {
  cycle.constructor === Object;
  cycle.constructor === Object;
  cycle.cycle === cycle;
  cycle.cycle.cycle === cycle;
  cycle.cycle.cycle.cycle === cycle;
  cycle.cycle.cycle.cycle.cycle === cycle;
  console.log("success");
} catch {
  console.log("Error");
  console.log("cycle:");
  console.log("Задание выполнено неверно");
}
